# 企业管理看板

**路径:** `~/.openclaw/workspace/services/enterprise_dashboard.py`  
**端口:** 5000  
**状态:** 🟢 运行中  
**归档时间:** 2026-04-23

---

## 一、概述

企业管理看板提供企业运行状态的 Web 展示界面。

## 二、代码

```python
#!/usr/bin/env python3
"""企业管理看板 - 端口5000"""
from http.server import HTTPServer, SimpleHTTPRequestHandler

html = """<!DOCTYPE html>
<html>
<head><meta charset="utf-8"><title>企业管理看板</title></head>
<body style="background:#0a0a0a;color:#00ff00;font-family:monospace;">
<h1>企业管理看板</h1>
<p>服务运行中...</p>
</body></html>"""

class Handler(SimpleHTTPRequestHandler):
    def do_GET(self):
        if self.path == '/' or self.path == '/index.html':
            self.send_response(200)
            self.send_header('Content-Type', 'text/html; charset=utf-8')
            self.end_headers()
            self.wfile.write(html.encode('utf-8'))
        else:
            super().do_GET()

if __name__ == "__main__":
    print("企业管理看板 启动于端口 5000")
    HTTPServer(('0.0.0.0', 5000), Handler).serve_forever()
```

## 三、快速访问

```
http://127.0.0.1:5000/
```